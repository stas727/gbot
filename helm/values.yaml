# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: stas727
  pullPolicy: IfNotPresent
  arch: amd64
  os: "linux"
  registry: "ghcr.io"
  tag: "v1.0.0-4397f03-linux-amd64"
imagePullSecrets:
  - name: stas727
