# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: stas727
  arch: amd64
  os: "linux"
  registry: "docker.io"
  tag: "v1.0.2-004fcb0-linux"
secret:
  name: "gbot"
  env: "TELE_TOKEN"
  key: "token"
securityContext:
  privileged: true
